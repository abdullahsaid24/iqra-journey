
import { ParentDashboard as ParentDashboardComponent } from "@/quran/components/parent/ParentDashboard";

const ParentDashboard = () => {
  return <ParentDashboardComponent />;
};

export default ParentDashboard;
